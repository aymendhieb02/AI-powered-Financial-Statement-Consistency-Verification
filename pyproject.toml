[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sicav-checker"
version = "0.1.0"
description = "Local SICAV financial statement consistency checker."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
sicav-checker = "sicav_checker.cli:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q -p no:cacheprovider"
