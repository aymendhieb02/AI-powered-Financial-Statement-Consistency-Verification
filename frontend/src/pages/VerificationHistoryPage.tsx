import { useEffect, useState } from 'react';
import { listHistory } from '../api/enterprise';
export function VerificationHistoryPage() { const [runs, setRuns] = useState<any[]>([]); useEffect(() => { listHistory().then(setRuns).catch(() => undefined); }, []); return <section className="space-y-5"><h1 className="text-2xl font-semibold">Verification History</h1><div className="rounded-lg border border-audit-line bg-white"><table className="w-full text-left text-sm"><thead className="bg-audit-panel text-audit-muted"><tr><th className="px-4 py-3">Run</th><th>Timestamp</th><th>Risk change</th><th>Confidence</th></tr></thead><tbody>{runs.map((run) => <tr className="border-t border-audit-line" key={run.run_id}><td className="px-4 py-3 font-mono">{run.run_id}</td><td>{run.timestamp}</td><td>{run.changed_risk_score}</td><td>{run.confidence ? Math.round(run.confidence.overall * 100) + '%' : '-'}</td></tr>)}</tbody></table></div></section>; }
