import { useEffect, useState } from 'react';
import { listRules } from '../api/enterprise';
export function RulesPage() { const [rules, setRules] = useState<any[]>([]); useEffect(() => { listRules().then(setRules).catch(() => undefined); }, []); return <section className="space-y-5"><h1 className="text-2xl font-semibold">Rules</h1><div className="rounded-lg border border-audit-line bg-white"><table className="w-full text-left text-sm"><thead className="bg-audit-panel text-audit-muted"><tr><th className="px-4 py-3">ID</th><th>Name</th><th>Severity</th><th>Expression</th></tr></thead><tbody>{rules.map((rule) => <tr className="border-t border-audit-line" key={rule.id}><td className="px-4 py-3 font-mono">{rule.id}</td><td>{rule.name}</td><td>{rule.severity}</td><td className="font-mono">{rule.expression}</td></tr>)}</tbody></table></div></section>; }
