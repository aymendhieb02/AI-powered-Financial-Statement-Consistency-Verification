export function Header() {
  return <header className="border-b border-audit-line bg-white px-6 py-4">
    <div className="text-sm text-audit-muted">Create Project → Upload PDFs → Run Verification → Review Anomalies → Download Report</div>
  </header>;
}
